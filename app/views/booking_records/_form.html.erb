<%= form_with(model: booking_record, local: true) do |form| %>
  <% if booking_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking_record.errors.count, "error") %> prohibited this booking_record from being saved:</h2>

      <ul>
      <% booking_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :booking_record_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :booking_record_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
